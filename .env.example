# Porta externa no host para o container de produção (Next.js standalone)
PORT=8080

# Porta externa no host para o container de desenvolvimento
DEV_PORT=3000

# URL pública do site (usada para tags OpenGraph, sitemap e robots)
NEXT_PUBLIC_SITE_URL=http://localhost:8080
